let stock = document.querySelectorAll(".stock");
stock.forEach((element) => {
  checkStock(element.textContent, element);
});
// let nameProduct = document.querySelectorAll(".name");
// console.log(nameProduct);

// nameProduct.forEach((element) => {
//   console.log(element.textContent.length);
//   if(nameProduct.textContent.length >)
// });
